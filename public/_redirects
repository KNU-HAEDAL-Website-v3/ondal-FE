# Cloudflare Pages SPA 폴백 - 정적 파일에 없는 경로는 index.html 을 200 으로 준다.
# 없으면 /problems/12 처럼 주소창에 직접 치거나 새로고침할 때 404 가 난다 (라우팅은 브라우저에서 일어나므로).
# 정적 자산(/assets/...)은 Pages 가 먼저 매칭하므로 이 규칙에 걸리지 않는다.
/*    /index.html   200
